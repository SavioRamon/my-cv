
const fonts = {
    smallFont: "calc(1.1rem + .5vw)",
    normalFont: "calc(1.3rem + .7vw)",
    largeFont: "calc(1.5rem + .9vw)",
    extraLargeFont: "calc(1.8rem + 1.1vw)"
};

export const metrics = {
    fonts
}

export type Metrics = typeof metrics;